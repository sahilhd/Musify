package com.presenters;

/**
 * An Enum to store available language for the system
 */
public enum LanguageType {
    ENGLISH,
    GERMAN,
    //as more languages are implemented, this enum list increases with supported languages
}
